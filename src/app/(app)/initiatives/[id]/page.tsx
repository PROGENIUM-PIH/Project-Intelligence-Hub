import { notFound } from "next/navigation";import { format } from "date-fns";import { FileText } from "lucide-react";import { prisma } from "@/lib/prisma";import { PageHeader } from "@/components/shared/page-header";import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card";import { Progress } from "@/components/ui/progress";import { StatusBadge } from "@/components/shared/status-badge";import { Tabs,TabsContent,TabsList,TabsTrigger } from "@/components/ui/tabs";import { healthTone,taskStatusTone,riskSeverityTone,meetingTypeLabel } from "@/lib/status";
export default async function InitiativeDetailPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const initiative=await prisma.initiative.findUnique({where:{id},include:{markets:{include:{market:true}},tasks:{orderBy:{dueDate:"asc"}},risks:{orderBy:{identifiedDate:"desc"}},meetings:{orderBy:{date:"desc"}},documents:{orderBy:{createdAt:"desc"}}}});if(!initiative)notFound();const {label,tone}=healthTone(initiative.status);return <div><PageHeader title={`${initiative.code} · ${initiative.name}`} description={`Owner: ${initiative.owner} · Target: ${format(initiative.targetDate,"MMM yyyy")}`} actions={<StatusBadge label={label} tone={tone}/>}/><Card className="mb-6"><CardContent className="space-y-4 p-6"><p className="text-sm text-muted-foreground">{initiative.description}</p><Progress value={initiative.progress}/></CardContent></Card><Tabs defaultValue="markets"><TabsList><TabsTrigger value="markets">Markets ({initiative.markets.length})</TabsTrigger><TabsTrigger value="documents">Documents ({initiative.documents.length})</TabsTrigger><TabsTrigger value="tasks">Tasks ({initiative.tasks.length})</TabsTrigger><TabsTrigger value="risks">Risks ({initiative.risks.length})</TabsTrigger><TabsTrigger value="meetings">Meetings ({initiative.meetings.length})</TabsTrigger></TabsList><TabsContent value="markets" className="mt-4"><Card><CardContent className="space-y-3 p-6">{initiative.markets.map(mi=><div key={mi.id} className="rounded-lg border p-3"><p className="text-sm font-medium">{mi.market.code} · {mi.market.name}</p><p className="text-xs text-muted-foreground">Local lead: {mi.localLead}</p></div>)}</CardContent></Card></TabsContent><TabsContent value="documents" className="mt-4"><Card><CardHeader><CardTitle className="text-base">Initiative Documents</CardTitle></CardHeader><CardContent className="space-y-3">{initiative.documents.length===0&&<p className="text-sm text-muted-foreground">No documents uploaded yet.</p>}{initiative.documents.map(d=><div key={d.id} className="flex items-center justify-between gap-3 rounded-lg border p-3"><div><p className="flex items-center gap-2 text-sm font-medium"><FileText className="h-4 w-4"/>{d.name}</p><p className="text-xs text-muted-foreground">Uploaded {format(d.createdAt,"MMM d, yyyy")} · {(d.size/1024/1024).toFixed(2)} MB</p></div><a className="text-sm font-medium underline" target="_blank" rel="noreferrer" href={`/api/documents/file?pathname=${encodeURIComponent(d.pathname)}`}>Open</a></div>)}</CardContent></Card></TabsContent><TabsContent value="tasks" className="mt-4"><Card><CardContent className="space-y-3 p-6">{initiative.tasks.map(t=>{const s=taskStatusTone(t.status);return <div key={t.id} className="flex justify-between rounded-lg border p-3"><span>{t.title}</span><StatusBadge label={s.label} tone={s.tone}/></div>})}</CardContent></Card></TabsContent><TabsContent value="risks" className="mt-4"><Card><CardContent className="space-y-3 p-6">{initiative.risks.map(r=>{const s=riskSeverityTone(r.severity);return <div key={r.id} className="flex justify-between rounded-lg border p-3"><span>{r.title}</span><StatusBadge label={s.label} tone={s.tone}/></div>})}</CardContent></Card></TabsContent><TabsContent value="meetings" className="mt-4"><Card><CardContent className="space-y-3 p-6">{initiative.meetings.map(m=><div key={m.id} className="rounded-lg border p-3"><p className="text-sm font-medium">{m.title}</p><p className="text-xs text-muted-foreground">{format(m.date,"EEE, MMM d yyyy")} · {meetingTypeLabel(m.type)}</p></div>)}</CardContent></Card></TabsContent></Tabs></div>}
