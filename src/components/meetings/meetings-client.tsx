"use client";
import {useMemo,useState} from "react";import{useRouter}from"next/navigation";import{format,isBefore,startOfDay}from"date-fns";import type{ColumnDef}from"@tanstack/react-table";import{Eye,FileUp,Pencil,Plus,Trash2}from"lucide-react";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{DataTable}from"@/components/shared/data-table";import{SearchBar}from"@/components/shared/search-bar";import{ConfirmDeleteDialog}from"@/components/shared/confirm-delete-dialog";import{MeetingForm}from"@/components/forms/meeting-form";import{CalendarImport}from"@/components/meetings/calendar-import";import{deleteMeeting}from"@/lib/actions/meetings";
export type MeetingRow={id:string;title:string;type:string;scope:string;date:Date;ap:string|null;notes:string;marketId:string|null;initiativeId:string|null;market:{id:string;code:string;name:string}|null;initiative:{id:string;code:string;name:string}|null};type Option={id:string;code:string;name:string};type Tab="UPCOMING"|"PAST";
export function MeetingsClient({meetings,markets,initiatives}:{meetings:MeetingRow[];markets:Option[];initiatives:Option[]}){const router=useRouter();const[tab,setTab]=useState<Tab>("UPCOMING");const[search,setSearch]=useState("");const[dialogOpen,setDialogOpen]=useState(false);const[editingMeeting,setEditingMeeting]=useState<MeetingRow|undefined>();const[viewingMeeting,setViewingMeeting]=useState<MeetingRow|null>(null);const[deleteTarget,setDeleteTarget]=useState<MeetingRow|null>(null);const[importOpen,setImportOpen]=useState(false);const today=startOfDay(new Date());const filtered=useMemo(()=>meetings.filter(m=>{const past=isBefore(new Date(m.date),today);if(tab==="UPCOMING"&&past)return false;if(tab==="PAST"&&!past)return false;const q=search.toLowerCase();return!q||m.notes.toLowerCase().includes(q)||(m.ap||"").toLowerCase().includes(q)||(m.market?.name||"").toLowerCase().includes(q)||(m.initiative?.name||"").toLowerCase().includes(q)}),[meetings,search,tab]);const columns:ColumnDef<MeetingRow>[]=[{accessorKey:"date",header:"Date",cell:({row})=><span className="text-sm">{format(new Date(row.original.date),"MMM d, yyyy")}</span>},{id:"market",header:"Market",cell:({row})=><span className="text-sm">{row.original.market?.code||"—"}</span>},{id:"initiative",header:"Initiative",cell:({row})=><span className="text-sm">{row.original.initiative?.code||"—"}</span>},{accessorKey:"ap",header:"AP",cell:({row})=><span className="text-sm">{row.original.ap||"—"}</span>},{accessorKey:"notes",header:"Notes",cell:({row})=><button className="max-w-md truncate text-left text-sm hover:underline" onClick={()=>setViewingMeeting(row.original)}>{row.original.notes||"—"}</button>},{id:"actions",header:"",enableSorting:false,cell:({row})=><div className="flex justify-end gap-1"><Button variant="ghost" size="icon" onClick={()=>setViewingMeeting(row.original)}><Eye className="h-4 w-4"/></Button><Button variant="ghost" size="icon" onClick={()=>{setEditingMeeting(row.original);setDialogOpen(true)}}><Pencil className="h-4 w-4"/></Button><Button variant="ghost" size="icon" className="text-destructive" onClick={()=>setDeleteTarget(row.original)}><Trash2 className="h-4 w-4"/></Button></div>}];return <div><div className="mb-5 flex flex-wrap items-center justify-between gap-3"><div className="flex rounded-lg border bg-background p-1">{(["UPCOMING","PAST"]as Tab[]).map(t=><Button key={t} size="sm" variant={tab===t?"default":"ghost"} onClick={()=>setTab(t)}>{t==="UPCOMING"?"Upcoming":"Past"}</Button>)}</div><div className="flex gap-2"><Button variant="outline" onClick={()=>setImportOpen(true)}><FileUp className="h-4 w-4"/>Import</Button><Button onClick={()=>{setEditingMeeting(undefined);setDialogOpen(true)}}><Plus className="h-4 w-4"/>Add Meeting</Button></div></div><SearchBar placeholder="Search meetings..." value={search} onChange={setSearch} className="mb-4 w-full sm:w-72"/><DataTable columns={columns} data={filtered} emptyMessage="No meetings found."/><Dialog open={!!viewingMeeting} onOpenChange={o=>!o&&setViewingMeeting(null)}><DialogContent className="sm:max-w-2xl"><DialogHeader><DialogTitle>Meeting details</DialogTitle></DialogHeader>{viewingMeeting&&<div className="space-y-4"><div className="grid gap-3 rounded-lg border p-4 text-sm sm:grid-cols-4"><div><p className="text-xs text-muted-foreground">Date</p><p>{format(new Date(viewingMeeting.date),"MMM d, yyyy")}</p></div><div><p className="text-xs text-muted-foreground">Market</p><p>{viewingMeeting.market?.code||"—"}</p></div><div><p className="text-xs text-muted-foreground">Initiative</p><p>{viewingMeeting.initiative?.code||"—"}</p></div><div><p className="text-xs text-muted-foreground">AP</p><p>{viewingMeeting.ap||"—"}</p></div></div><div><p className="mb-2 font-semibold">Notes</p><div className="whitespace-pre-wrap rounded-lg border p-4 text-sm">{viewingMeeting.notes}</div></div></div>}</DialogContent></Dialog><Dialog open={dialogOpen} onOpenChange={setDialogOpen}><DialogContent className="sm:max-w-lg"><DialogHeader><DialogTitle>{editingMeeting?"Edit Meeting":"Add Meeting"}</DialogTitle></DialogHeader><MeetingForm markets={markets} initiatives={initiatives} meeting={editingMeeting} onSuccess={()=>{setDialogOpen(false);router.refresh()}}/></DialogContent></Dialog><Dialog open={importOpen} onOpenChange={setImportOpen}><DialogContent className="max-h-[92vh] overflow-y-auto sm:max-w-4xl"><DialogHeader><DialogTitle>Import calendar meetings</DialogTitle></DialogHeader><CalendarImport markets={markets} initiatives={initiatives} onImported={()=>{setImportOpen(false);router.refresh()}}/></DialogContent></Dialog><ConfirmDeleteDialog open={!!deleteTarget} onOpenChange={o=>!o&&setDeleteTarget(null)} title="Delete meeting" description="Are you sure you want to delete this meeting?" onConfirm={async()=>{if(!deleteTarget)return;await deleteMeeting(deleteTarget.id);setDeleteTarget(null);router.refresh()}}/></div>}
